spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/workly_db_test?sslmode=disable
    username: workly
    password: workly_pass

workly:
  security:
    secret: 'SecretForJWT'
    expiration-time: 1800000
  swagger:
    host: 'localhost:8080'
    protocol: 'http'
