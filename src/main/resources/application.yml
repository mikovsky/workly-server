spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DATABASE_URL}:5432/worklydb?sslmode=disable
    username: ${DATABASE_USER}
    password: ${DATABASE_PASS}
